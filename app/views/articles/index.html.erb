<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Wikinimous</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <button class="btn btn-outline-success border-0" type="submit"><%= link_to "Add a new article", new_article_path %></button>
    </div>
  </div>
</nav>

<div class="container">
  <h1>Articles</h1>
</div>

<div class="container">
  <ul class="list-unstyled">
    <% @articles.each do |article| %>
      <li>
        <h2 class="fs-4 text-center"><%= link_to article.title, article_path(article) %></h2>
        <p class="text-center"><%= article.content %></p>
      </li>
      <% end %>
  </ul>
</div>
